/**
 * CSS-only entry point, published as `@branchleft/components/css`.
 *
 * Import this once in your app if you use `<ValuesCloud>` — it's the only
 * component in the package that ships structural CSS (see
 * `components/ValuesCloud.css` for why). Every other component is headless
 * and needs no import here.
 */
import './components/ValuesCloud.css';
