# Repo-specific ignores. Everything this file used to list — node_modules,
# dist, storybook-static, coverage, graphify-out, sibling worktrees, the
# lockfile, minified assets — is in the shared set at
# node_modules/@branchleft/prettier-config/prettierignore, which the format
# scripts pass as a third --ignore-path. Nothing here is repo-specific today.
#
# The file is kept rather than deleted because the scripts name it explicitly,
# and Prettier tolerates a missing --ignore-path silently — so deleting it
# would work now and fail quietly the day someone adds an entry expecting it to
# be read.
